# Orbius — Source File Index

Complete inventory of every project file. Line counts approximate (Aug 2026).

---

## Project Root

| File | Lines | Purpose |
|------|-------|---------|
| `package.json` | 20 | Dependencies, npm scripts |
| `package-lock.json` | — | Lockfile (regenerated by `npm install`) |
| `vite.config.js` | 7 | Vite + React plugin |
| `index.html` | 17 | SPA shell, favicon links, `#root`, entry script |
| `.gitignore` | 3 | Ignores `node_modules/`, `dist/`, `.env` |

---

## `public/`

| File | Purpose |
|------|---------|
| `favicon.svg` | Browser tab icon (gradient infinity mark) |
| `favicon.ico` | Legacy favicon |
| `icons/icon-180.png` | Apple touch icon |
| `icons/icon-192.png` | PWA manifest icon |
| `icons/icon-512.png` | PWA splash / install icon |

---

## `src/`

| File | Lines | Purpose |
|------|-------|---------|
| `main.jsx` | 12 | ReactDOM root, StrictMode, KeyboardLayerProvider, global.css |
| `App.jsx` | 440 | Tab nav, Logo header, settings (profile, calories, export/import confirm/wipe), persist banner, localStorage, progressMounted, page routing |
| `App.module.css` | 320 | `.app`, `.header`, `.logo`, `.settingsBtn`, `.persistBanner`, settings form classes, `.navTrack`, `.navBtn`, `.main` |

## `src/domain/storage.js`

**~250 lines** — `normalizeStoredData()`, `parseImportedData()`, `wipeLogs()`, `patchSettings()`, `mergePersistedData()`, `persistStoredData()`, `profilePrefill()`, `applyProfilePrefill()`, export file name. Spec: `spec/storage-fixtures.json`. Used by `App.jsx`, Body Comp, and Calories.

## `src/domain/inbodyCsv.js`

**~270 lines** — `parseInbodyCsv()` (same-day last-timestamp wins; incomplete rows skipped), `buildInbodyEntry()`, `mergeInbodyIntoLogs()` (replaces every row for a date). Spec: `spec/inbody-csv-fixtures.json`. Used by Body Comp History import.

## `src/domain/bodyTrends.js`

**~140 lines** — `buildAllBodyTrendSeries()` maps `bodyComp` onto Progress visceral, BMR, fat, muscle, fat-free, composition, score, and segmental trend charts. `buildSegmentalGridModel()` places those series on the Progress body grid with shared arm/leg Y-axes. `resolveSegmentalTrendGroup()` picks Soft Lean vs Fat for the Progress toggle. Spec: `spec/inbody-csv-fixtures.json` `trendFixtures` / `gridFixtures` / `gridToggleFixtures` + `spec/page-layout.json` `visceralTrends` / `bmrTrends` / `fatTrends` / `muscleTrends` / `ffmTrends` / `compositionTrends` / `scoreTrends` / `segmentalTrendGroups` / `segmentalBodyGrid`.

## `src/domain/bodySegmental.js`

**~70 lines** — `buildSegmentalSnapshot()`, `latestSegmentalSnapshot()`. Maps stored `inbody.lean*` / `fat*` fields onto the Body Comp segmental figure. Spec: `spec/inbody-csv-fixtures.json` `segmentalFixtures` + `spec/page-layout.json` `segmentalMap`.

---

## `src/styles/`

| File | Lines | Purpose |
|------|-------|---------|
| `theme.css` | 52 | All `--ft-*` CSS custom properties (colors, bg, borders, radii, shadows) |
| `global.css` | 100 | Imports theme; html/body reset; number-input rules; `.ft-kb-*` keyboard classes |
| `styleHelpers.js` | 93 | `cx()`, `btnPrimaryClass/Secondary/Danger()`, `inputClass/selectClass/textareaClass()` |
| `ui.module.css` | 545 | Shared UI: buttons, inputs, cards, modals, collapse, pill toggles, chart toggles, utilities |

---

## `src/components/Logo.jsx`

**~35 lines** — Orbius brand mark (gradient infinity loop). Spec: `page-layout.json` → `app.logo`. Used in `App.jsx` header with `rounded` dark tile.

## `src/components/PageIcon.jsx`

**~45 lines** — Lucide stroke icons for page titles, collapses, and the header settings gear (`PageIcon`, `PageHeading`). Spec: `spec/page-icons.json` + `pageIcon` / `hero.icon` / collapse `icon` / `app.settings.icon` in `page-layout.json`. Domain: `src/domain/pageIcons.js`.

---

## `src/components/shared.jsx`

**~722 lines** — central module. All pages import from here.

### Re-exported styling

`ui`, `cx`, `btnPrimary`, `btnSecondary`, `btnDanger`, `inp`, `inputClass`, `selectClass`, `textareaClass`, `btnPrimaryClass`, etc.

### Constants

| Export | Description |
|--------|-------------|
| `ACCENT`, `GREEN`, `PINK`, `ORANGE`, `BLUE` | Hex color constants |
| `EXERCISE_CATEGORIES` | 7 categories × 10 exercises |
| `ALL_EXERCISES` | Flattened exercise list |
| `NO_SPLIT_LIFTS` | 15 bilateral compounds (no L/R split charts) |
| `COMPOUND_LIFTS` | 23 compound exercise names |
| `ACTIVITY` | 5 TDEE activity levels with multipliers |
| `MONTH_MAP` | Month name/abbrev → 0–11 |
| `EXERCISE_CHART_COLORS` | 6 fixed lift colors |

### Functions

| Export | Description |
|--------|-------------|
| `resolveExercise(raw)` | Alias + fuzzy match → canonical name |
| `isNoSplitLift(exercise)` | In NO_SPLIT_LIFTS? |
| `isCompoundLift(exercise)` | In COMPOUND_LIFTS? |
| `formatExerciseName(exercise)` | Display name (CAPS for compounds) |
| `getExerciseChartColor(exercise, fallbackIdx?)` | Chart line color |
| `formatDate(date)` | → `DD-MM-YYYY` |
| `parseWorkoutText(text)` | Smart parser → `{ date, entries[] }` |
| `restStr(t1, t2)` | Rest duration between MM:SS times |
| `isTypingTarget(el)` | Input focus check for keyboard |
| `kbItemClass(index, focusIdx, activatedIdx)` | Keyboard highlight classes |

### Keyboard exports

| Export | Description |
|--------|-------------|
| `KeyboardLayerProvider` | Context provider wrapping app |
| `useKeyboardLayer(id, open, handler)` | Popup layer registration |
| `useKeyboardLayersBlocked()` | Any layer open? |
| `useKeyboardListNav(count, onSelect, enabled)` | ↑↓ Enter list nav |
| `useDisableNumberInputWheel()` | Block wheel on number inputs |
| `useAppNavKeyboard(tabs, currentTab, setTab)` | 1–5, ←→ nav shortcuts |
| `handleParserTextareaKeyDown(e, onSubmit)` | Enter to parse |
| `useParserTextareaKeyboard(ref, onSubmit, active)` | Textarea Enter listener |
| `useConfirmDialogKeyboard(...)` | Two-button confirm keys |

### UI components

| Export | Props |
|--------|-------|
| `Card` | `children`, `className?` |
| `StatBox` | `label`, `value`, `unit`, `color?` |
| `Collapse` | `emoji`, `icon`, `label`, `defaultOpen?`, `children` |

### Internal (not exported)

- `EXERCISE_ALIASES` object (~70 aliases) — must copy from source for exact parser behavior
- Parser helpers: `parseRepsFromBody`, `appendSet`, etc.

---

## `src/components/DashboardPage.jsx`

**~45 lines** | CSS: `DashboardPage.module.css` (~45 lines)

**Props:** `data`, `setTab`

**Features:**
- 4× `StatBox`: last body weight, last BF%, today calories, workout count
- PR list: max weight per exercise (`resolveExercise` grouping)
- Recent workouts: last 3 reversed
- `useKeyboardListNav` on PR + recent lists

**CSS classes:** `pageTitle`, `statRow`, `prRow`, `prValue`, `recentRow`, `recentExName`, `recentMeta`, `recentSets`, `emptyLink`

---

## `src/components/WorkoutPage.jsx`

**~1019 lines** | CSS: `WorkoutPage.module.css` (~804 lines)

**Props:** `data`, `save`

**Inner component:** `OneRMCalc({ data })`

**Major state:** `cat`, `ex`, `sets`, `logDate`, `logTime`, `showCalendarModal`, `calPanel`, `historySortBy`, `historyOrder`, `expandedGroups`, `groupsDefaultExpanded`, `editIdx`, `editForm`, parser texts

**Key functions:** `submit()`, `doParse()`, `calSubmit()`, `calDoParse()`, `saveEdit()`, `delW()`, `startEdit()`, `toggleGroup()`, `clearHistory()`, `parseDateString()`, `sortHistoryWorkouts()`

**Sections:** Hero, Quick actions, Calendar modal (month/year), Smart Parser modal, Log Workout collapse, History collapse, 1RM collapse

**CSS groups:** hero (filled stat strip), calendar (`calModal*`, `calDayCell`, `miniMonth*`), day panels, parser modal, history groups, 1RM calc, set picker, confirm dialog

Spec: [workout.md](./workout.md)

---

## `src/components/BodyCompPage.jsx`

**~450 lines** | CSS: `BodyCompPage.module.css` (~350 lines)

**Props:** `data`, `save`

**Key functions:**
- `computeBodyCompEntry(fields)` — all derived metrics
- `preserveMeasuredInbody(old, next)` — keep InBody extras on edit
- `upsertBodyCompByDate()` / `upsertBodyLogByDate()` — one row per date
- `compIdxFromDisplay(displayIdx)` — history is reversed display

**Features:** Log Entry collapse, live metric preview, sex pill toggle, history (all entries, newest first), InBody CSV import (merge by date), segmental lean/fat body map, inline edit, delete, clear-all confirm

Spec: [body-comp.md](./body-comp.md)

---

## `src/components/CaloriePage.jsx`

**~104 lines** | CSS: `CaloriePage.module.css` (~299 lines)

**Props:** `data`, `save`

**State:** `goal` (default 2200), `actIdx` (default 2), `selDate`, `editIdx`, `editForm`, food form fields

**Features:** BMR/TDEE display, activity selector, TEF/NEAT/EAT/PAEE breakdown, date picker calendar, macro totals, goal progress bar, custom entry collapse, food log edit/delete

Spec: [calories.md](./calories.md)

---

## `src/components/ProgressPage.jsx`

**~1109 lines** | CSS: `ProgressPage.module.css` (~454 lines)

**Props:** `data` (read-only)

### Exported

`default function ProgressPage({ data })`

### Internal components

| Name | Purpose |
|------|---------|
| `ExerciseChart` | Per-exercise card |
| `MemoExerciseChart` | `memo(ExerciseChart)` |
| `WorkoutSessionGraph` | Session performance popup |
| `ExerciseSplitDetail` | Docked L/R imbalance panel (exercise charts) |
| `SessionSplitSetDetail` | Docked L/R panel (session graph) |
| `SplitDot`, `SessionSetDot`, `FailedSetDot` | Custom Recharts dots |

### Internal helpers

| Function | Purpose |
|----------|---------|
| `parseChartDate(value)` | Multi-format date parse |
| `getChartDateKeyFromValue(value)` | Normalized YYYY-MM-DD key |
| `formatChartDateLabel(value)` | Chart X-axis label |
| `useInView(rootMargin?)` | IntersectionObserver lazy mount |
| `estimate1RM(w, r)` | Epley formula |
| `roundE1RM(v)` | 1 decimal place |
| `computeSessionMetrics(sets)` | Per-day metrics + L/R splits |
| `buildExerciseChartPoints(sessions, getChartDateKey)` | One point per date |
| `withTrendPlotValue(data, valueKey)` | Copies metric → `plotValue` for morph |
| `sessionChartYDomain(plotData, metric)` | Y-axis with 10% padding |
| `compoundChartYDomain(...)` | Multi-line Y domain |
| `useTrendChartAnimation(chartReady)` | 0ms first paint, 600ms toggle |
| `metricLabelFor(metric, session?)` | Toggle/tooltip labels |
| `formatChartTooltipValue(value, metric)` | Tooltip value formatting |
| `renderDetailSetRows`, `renderWorkoutSetPanel` | Day modal set list |
| `buildSetChartData(sets)` | Session graph points |
| `withFailedPlot(data, metric)` | Failed set Y positions |

### State (ProgressPage)

`compoundMetric`, `hiddenCompoundLifts`, `selectedDate`, `sessionGraphIdx`, footer `useInView`

Spec: [progress.md](./progress.md)

---

## `src/components/ProgressPage.module.css`

Key class groups:

| Group | Classes |
|-------|---------|
| Layout | `pageTitle`, `sectionLabel`, `exerciseCard`, `exerciseHeader` |
| Stats | `prBlock`, `statStrip`, `miniStat*` |
| Charts | `chartPlaceholder`, `splitRow`, `splitCol`, `splitLabel`, `segHeader`, `segGrid`, `segFigure` |
| Toggles | `splitToggleGroup`, `compoundMetricToggleActive`, `compoundLegendRow` |
| Modals | `detailBackdrop`, `detailPanel`, `sessionGraphOverlay`, `sessionGraphPanel` |
| Sets | `setRow`, `setRowBest`, `setRowE1rm`, `setRowE1rmBest` |
| Split UI | `sessionSplitDetail*`, `imbalanceHint`, `imbalanceDot` |
| Tooltips | `tooltip`, `tooltipTitle` |

---

## `docs/`

| File | Purpose |
|------|---------|
| `RECONSTRUCTION.md` | **Master rebuild guide** |
| `README.md` | Feature doc index |
| `architecture.md` | System design |
| `data-model.md` | Full schema |
| `source-index.md` | This file |
| `dashboard.md` | Dashboard page spec |
| `workout.md` | Workout page spec |
| `body-comp.md` | Body comp spec |
| `calories.md` | Calories spec |
| `progress.md` | Progress charts spec |
| `styling.md` | CSS conventions |
| `shared-utilities.md` | Parser & helpers spec |
| `keyboard-navigation.md` | Keyboard spec |

---

## Import Cheat Sheet

```javascript
// Typical page imports
import { useState, useMemo } from "react";
import { ACCENT, GREEN, Card, Collapse, btnPrimary, inp, ui, cx, ... } from "./shared.jsx";
import s from "./WorkoutPage.module.css";

// Progress only
import { ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip, CartesianGrid } from "recharts";

// App only
import { useAppNavKeyboard, useDisableNumberInputWheel } from "./components/shared.jsx";
import styles from "./App.module.css";
```

---

## Files NOT in Repository

These are generated or local-only:

| Path | Generated by |
|------|--------------|
| `node_modules/` | `npm install` |
| `dist/` | `npm run build` |
| `.env` | Not used (ignored) |
